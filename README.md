# مستخرج الأسئلة من الملفات

موقع ويب بسيط يستخدم الذكاء الاصطناعي (Gemini AI) لاستخراج الأسئلة من الملفات النصية تلقائياً.

## المميزات

- 📤 رفع ملفات نصية (.txt, .md)
- 🤖 استخراج الأسئلة تلقائياً باستخدام Gemini AI
- 💾 حفظ الملفات والأسئلة في قاعدة البيانات
- 🔐 نظام تسجيل دخول آمن
- 📱 واجهة مستخدم بسيطة وسهلة الاستخدام

## التقنيات المستخدمة

- **Frontend**: React 19 + Tailwind CSS 4
- **Backend**: Express.js + tRPC
- **Database**: MySQL/TiDB
- **AI**: Google Gemini API
- **Authentication**: Manus OAuth

## المتطلبات

- Node.js 22+
- pnpm
- Gemini API Key من [Google AI Studio](https://aistudio.google.com/app/apikey)

## التثبيت والتشغيل

1. استنساخ المستودع:
```bash
git clone https://github.com/MoainAlabbasi/file-question-extractor.git
cd file-question-extractor
```

2. تثبيت الحزم:
```bash
pnpm install
```

3. إعداد متغيرات البيئة:
```bash
# أضف GEMINI_API_KEY إلى ملف .env
GEMINI_API_KEY=your_api_key_here
```

4. تطبيق قاعدة البيانات:
```bash
pnpm db:push
```

5. تشغيل المشروع:
```bash
pnpm dev
```

## الاستخدام

1. قم بتسجيل الدخول
2. اختر ملف نصي (.txt أو .md)
3. اضغط على "استخراج الأسئلة"
4. شاهد الأسئلة المستخرجة تلقائياً!

## الاختبارات

```bash
pnpm test
```

## الترخيص

MIT License
